# Final Acceptance Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correct CSDID custom-cluster postestimation, preserve strict HDFE slope regression coverage, and leave the branch in a reproducible delivery state.

**Architecture:** CSDID will have one internal covariance path that converts unit-level influence-function columns into a covariance matrix, aggregating by the fitted cluster mapping when applicable. Every postestimation aggregation will retain its IF vector and use that path for standard errors, VCE, and Wald tests. HDFE tests will separate supported slope fields from the explicitly unresolved constant field.

**Tech Stack:** Python 3.11, NumPy, pandas, SciPy, pytest, local Stata 17 Golden tests.

---

### Task 1: CSDID Cluster Covariance Regression Tests

**Files:**
- Modify: `tests/test_compat_stata_did.py`

- [ ] Add a deterministic panel with multiple units per custom cluster.
- [ ] Add failing tests for `estat simple`, `group`, `calendar`, and `pretrend` against manually cluster-aggregated IF covariance.
- [ ] Run the targeted tests and confirm they fail for covariance/SE differences.

### Task 2: Unified CSDID IF Covariance

**Files:**
- Modify: `src/stataflow/estimators/csdid.py`

- [ ] Add a focused helper that returns covariance from one or more unit-level IF vectors.
- [ ] Build and cache the unit-to-cluster projection once after fitting.
- [ ] Replace duplicated event covariance logic with the helper.
- [ ] Preserve IF vectors for simple, group, and calendar aggregations and build full VCE matrices.
- [ ] Use the same covariance in pretrend Wald inference.
- [ ] Run targeted CSDID tests until green, then run all DID tests.

### Task 3: HDFE Golden Test Isolation

**Files:**
- Modify: `tests/golden/test_w7_reghdfe_2way_cluster.py`
- Modify: `tests/golden/test_w7_reghdfe_2way_cluster_real.py`

- [ ] Replace function-level `xfail` with normal slope SE tests at `<1e-6`.
- [ ] Keep the unresolved `_cons` comparison in a dedicated strict expected-failure test, pending the documented kernel redesign.
- [ ] Run both Golden files and confirm slope checks are ordinary passes.

### Task 4: Documentation and Delivery Hygiene

**Files:**
- Modify: `docs/audit/revalidation-v1.2/REMEDIATION_REPORT.md`
- Modify: `docs/audit/revalidation-v1.2/FINAL_ACCEPTANCE_REPORT.md`
- Modify: `docs/audit/revalidation-v1.2/FINAL_REWORK_TASKS.md`
- Review: `stata/cases/check_install.do`

- [ ] Record the actual CSDID fix and residual HDFE limitation without claiming full strict equivalence.
- [ ] Remove obsolete or temporary local artifacts only when clearly generated by this task.
- [ ] Ensure `git diff --check` succeeds for newly changed files.

### Task 5: Full Verification

- [ ] Run non-Golden pytest.
- [ ] Run complete Stata Golden pytest.
- [ ] Run compileall and wheel build.
- [ ] Run `git diff --check` and inspect `git status --short`.
- [ ] Do not push unless all required gates and GitHub authentication pass.
